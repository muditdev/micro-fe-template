import React from 'react'

function Welcome({ user }: { user: string }) {
    return (
        <div>
            Welcome  { user} to the portal !
        </div>
    )
}

export default Welcome;
